<%= form_for(voucher, html: { class: 'ui form' }) do |f| %>
  <% if voucher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(voucher.errors.count, "error") %> prohibited this voucher from being saved:</h2>

      <ul>
      <% voucher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Codigo_Usuario_de_la_Empresa %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :salario %>
    <%= f.number_field :salary %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
